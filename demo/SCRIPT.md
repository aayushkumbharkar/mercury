# Mercury — voiceover script

Target duration: 1:57. Read calmly; leave the gaps for the visual evidence. The recorded workflow uses real Google APIs with a disclosed controlled stale write.

**0:00**

Changing one launch date sounds simple. Across a calendar, a task register, and a launch brief, it can leave a team working from conflicting information.

**0:13**

Mercury takes one launch change and discovers the linked records in Google Calendar, Sheets, and Docs. These are real applications. The graph shows three task owners and their prerequisites. Declared offsets keep quality checks before release, rather than moving every task to the same day.

**0:37**

Now Mercury applies the plan. Every write has a separate readback. An accepted request is not evidence that the desired state exists.

**0:50**

This run deliberately injects one stale write. The API acknowledges it, but the recorded date is still wrong. Mercury withholds success and checks whether repair is safe.

**1:08**

One bounded repair restores the expected date. The proof shows the contradiction: September sixteenth was observed when September twentieth was required. A new read verifies September twentieth. Only then can dependent work proceed.

**1:29**

The final receipt records five verified postconditions across all three applications, with timestamps and external records. Missing evidence stays inconclusive. A conflicting edit stops automatic repair.

**1:44**

This is recovery within a defined workflow, not a promise that everybody read an update. Mercury shows what changed, what failed, what recovered, and what is proven.
